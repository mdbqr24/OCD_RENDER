[phases.setup]
nixpkgsPackages = ["python311"]

[phases.install]
commands = [
  "python3 -m ensurepip --upgrade",
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[phases.start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"

[variables]
PORT = "8080"
